node_modules
dist
.env
.env.local
.env.*.local
.dev-tenants.json
*.log
coverage
.DS_Store
*.pem

# Tauri build output
src-tauri/target/

# Cloudflare tunnel credentials (never commit)
.cloudflared/*.json

# Deployment logs
.deploy-logs/

# Server data — customer data must never be in git
tools/data/
tools/subscriptions.json
tools/errors.json
tools/server.log
tools/tunnel.log

# AI model files — too large for git (copy with the project folder manually)
tools/ollama/models/
tools/kokoro/models/
tools/models/

# APK binary — uploaded to GitHub Releases instead
*.apk

# Tauri Android generated files
src-tauri/gen/
.claude/worktrees/
